# Working Tree Diff
# Includes unstaged and staged changes.
# Generated by iProgrammer Flow for execution exec-b37d0704-c058-41a2-acd8-cb76980d3794.

## Unstaged Diff
diff --git a/src/App.css b/src/App.css
index 087307b..5323765 100644
--- a/src/App.css
+++ b/src/App.css
@@ -225,6 +225,38 @@
   }
 }
 
+/* Validation & Hint Styles */
+.validation-error {
+  color: #ff6b6b;
+  font-size: 12px;
+  margin-top: 4px;
+}
+
+.password-hint {
+  color: #8892b0;
+  font-size: 12px;
+  margin-top: 4px;
+  display: block;
+}
+
+/* Register Link on Login Page */
+.register-link {
+  text-align: center;
+  margin-top: 20px;
+  color: #8892b0;
+  font-size: 14px;
+}
+
+.register-link-action {
+  color: #1d63b8;
+  cursor: pointer;
+  transition: color 0.2s ease;
+}
+
+.register-link-action:hover {
+  color: #61dafb;
+}
+
 /* Mobile Responsiveness */
 @media (max-width: 480px) {
   .login-card {
diff --git a/src/App.js b/src/App.js
index 771397c..4ffb324 100644
--- a/src/App.js
+++ b/src/App.js
@@ -1,6 +1,7 @@
 import React, { useState, useEffect, useCallback } from "react";
 import "./App.css";
 import Login from "./Login";
+import Register from "./Register";
 import Dashboard from "./Dashboard";
 
 const App = () => {
@@ -28,11 +29,15 @@ const App = () => {
 
   useEffect(() => {
     if (!isAuthenticated) {
-      if (pathname !== "/login") {
+      if (pathname !== "/login" && pathname !== "/register") {
         navigateTo("/login");
       }
     } else {
-      if (pathname === "/login" || pathname === "/") {
+      if (
+        pathname === "/login" ||
+        pathname === "/register" ||
+        pathname === "/"
+      ) {
         navigateTo("/dashboard");
       }
     }
@@ -41,7 +46,10 @@ const App = () => {
   // Determine what view to render based on authentication status
   const renderView = () => {
     if (!isAuthenticated) {
-      return <Login onLoginSuccess={() => navigateTo("/dashboard")} />;
+      if (pathname === "/register") {
+        return <Register navigateTo={navigateTo} />;
+      }
+      return <Login onLoginSuccess={() => navigateTo("/dashboard")} navigateTo={navigateTo} />;
     }
     return <Dashboard onLogout={() => navigateTo("/login")} />;
   };
diff --git a/src/Login.js b/src/Login.js
index dc1ad93..643b5d4 100644
--- a/src/Login.js
+++ b/src/Login.js
@@ -1,6 +1,6 @@
 import React, { useState } from "react";
 
-const Login = ({ onLoginSuccess }) => {
+const Login = ({ onLoginSuccess, navigateTo }) => {
   const [email, setEmail] = useState("");
   const [password, setPassword] = useState("");
 
@@ -50,6 +50,22 @@ const Login = ({ onLoginSuccess }) => {
           <button type="submit" className="login-submit-btn" aria-label="Sign In">
             Sign In
           </button>
+          <div className="register-link">
+            Don't have an account?{" "}
+            <span
+              className="register-link-action"
+              onClick={() => navigateTo("/register")}
+              role="button"
+              tabIndex={0}
+              onKeyDown={(e) => {
+                if (e.key === "Enter" || e.key === " ") {
+                  navigateTo("/register");
+                }
+              }}
+            >
+              Register
+            </span>
+          </div>
         </form>
       </div>
     </div>

## Staged Diff

(No staged index changes.)
